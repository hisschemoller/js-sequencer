/**
 * Plugin base objects. These are abstract and only meant to be extended.
 * PluginBase defines the base plugin properties that all plugin types use.
 * PluginGenerator is the base sound generator plugin.
 * PluginProcessor is the base sound processing plugin.
 * PluginAnalyser is the base sound analysis plugin.
 *
 * @namespace WH
 */
window.WH = window.WH || {};

(function (WH) {

    function pluginAbstract() {
        
    }

})(WH);
